[pytest]
addopts =
    -v
    --color=yes
    --doctest-modules
doctest_optionflags= NORMALIZE_WHITESPACE
python_files = *.py
testpaths = ../src/konfoo
norecursedirs = .* *.egg *.egg-info assets docs
